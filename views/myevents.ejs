<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .myevent ul li {
            display: flex;
            justify-content: space-between;
            border: 1px solid white;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgb(0, 0, 0, 0.1);
            margin: 15px 80px;
            padding: 15px;
        }
        
        .title {
            font-size: large;
            font-weight: bold;
            color: rgb(37, 60, 104);
        }
        
        .change-btn {
            display: flex;
            align-items: center;
        }
        
        .change-btn a,
        .change-btn button {
            margin: 0 5px;
        }
        
        #delete-btn {
            margin: 0 5px;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Tangerine" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Lemon&family=Monoton&family=Montserrat:ital,wght@0,200;0,400;0,500;1,300&display=swap"
        rel="stylesheet" />
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include('navbarcl') %>
    <h1 class="text-center mt-3">MY EVENTS</h1>
    <p class="text-center mt-3">You are allowed to edit only few details (FAQs,Payment and Registeration Form Editing is not allowed)</p>
    <!--Update an events
    while updating fetch all the events display them like lists beside for every event add two buttons delete update then according id of that event if they click on update again display them give edit option and submit
    delete just based on id of that database document-->
    <!--Delete an event-->
    <div class="myevent">
        <ul>
            <% events.forEach(event => { %>
            <li>
                <div class="title">
                    <%= event.eventname %>
                </div>
  
                    <div class="change-btn">
                        <form action="/editevent/<%= event._id %>" method="get" id="edit-btn">
                            <button type="submit" class="btn btn-dark view-details" id="edit-btn"><i class="fas fa-edit"></i></button>
                        </form>
                        <form action="/deleteevent" method="post" id="delete-btn">
                            <input type="hidden" name="itemId" value="<%= event._id %>">
                            <button type="submit" class="btn btn-dark view-details" id="delete-btn"><i class="fa fa-trash"
                                    aria-hidden="true"></i></button>
                        </form>
                    </div>
                  
            </li>
            <% }) %>
        </ul>
    </div>
</body>

</html>
