<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Announcement</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Tangerine" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Lemon&family=Monoton&family=Montserrat:ital,wght@0,200;0,400;0,500;1,300&display=swap"
        rel="stylesheet" />
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .myevent ul li {
            display: flex;
            justify-content: space-between;
            border: 1px solid white;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgb(0, 0, 0, 0.1);
            margin: 15px 80px;
            padding: 15px;
        }

        .title {
            font-size: large;
            font-weight: bold;
            color: rgb(37, 60, 104);
        }

        .change-btn {
            display: flex;
            align-items: center;
        }

        .change-btn a,
        .change-btn button {
            margin: 0 5px;
        }

        #delete-btn {
            margin: 0 5px;
        }
    </style>
</head>

<body>
    <%- include('navbarcl') %>
        <div class="container" id="main-container">
            <div class="container mt-5" id="form-container">
                <h1 class="text-center">ADD ANNOUNCEMENT</h1>
                <form class="mt-4" action="/addannouncement" method="post">
                    <div class="form-group">
                        <label for="username" class="text-primary">Club Name*</label>
                        <input type="text" class="form-control" id="username" name="username"
                            placeholder="Enter your Club Name" required>
                    </div>
                    <div class="form-group">
                        <label for="Announcement" class="text-primary">Announcement</label>
                        <textarea class="form-control" id="Announcement" name="announcement" rows="3"
                            placeholder="Add Announcement of your event" required></textarea>
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary mt-2">Submit</button>
                    </div>
                </form>
            </div>
            <h1 class="text-center mt-3">MY ANNOUNCEMENTS</h1>
            <div class="myevent">
            <ul>
                <% announcements.forEach(announce=> { %>
                    <li>
                        <div class="title">
                            <%= announce.announcement %>
                        </div>

                        <div class="change-btn">
                            <form action="/deleteannouncement" method="post" id="delete-btn">
                                <input type="hidden" name="itemId" value="<%= announce._id %>">
                                <button type="submit" class="btn btn-dark view-details" id="delete-btn"><i
                                        class="fa fa-trash" aria-hidden="true"></i></button>
                            </form>
                        </div>

                    </li>
                    <% }) %>
            </ul>
        </div>
        </div>
</body>

</html>